<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Read-only access to the device audio index, used by
         MediaStoreDeviceAudioScanner so we can see music in Downloads/,
         internal-storage root, and other places SAF refuses to grant tree-URI
         access to on Android 11+. Tag-writing (the "burn metadata" feature)
         only works on files inside folders the user has SAF-picked, where
         we get write access via the granted tree URI. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".OfflinePlayaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OfflinePlaya"
        tools:targetApi="31">

        <!-- Android Auto capability declaration. Points at
             automotive_app_desc.xml listing which Auto features we
             implement (media browser only for v1). -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OfflinePlaya">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <!-- MediaLibraryService is a strict superset of
                     MediaSessionService — both actions are valid so existing
                     phone clients keep binding. The MediaBrowserService
                     action is the legacy compat name older Auto clients
                     still look for. -->
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!--
            Exposes cacheDir/track_art/<key> as content:// URIs so Android
            Auto (and any other cross-process surface) can fetch album art
            without read-access to our private cache. See AppArtFileProvider.
            grantUriPermissions=true so we can stamp temporary access tokens
            on individual MediaItem metadata.artworkUri values.
        -->
        <provider
            android:name=".auto.AppArtFileProvider"
            android:authorities="${applicationId}.artprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

    </application>

</manifest>
